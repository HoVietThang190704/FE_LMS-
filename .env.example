# Frontend (.env.example)
# Copy to .env.local and fill values for local development. Do NOT commit .env.local with real secrets.

# Default locale (en or vi)
NEXT_PUBLIC_DEFAULT_LOCALE=en

# Backend / API
NEXT_PUBLIC_BACKEND_URL=http://localhost:5000
NEXT_PUBLIC_API_BASE=
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional third-party / services
NEXT_PUBLIC_PISTOL_API_BASE=https://api.judge0.com
NEXT_PUBLIC_SOCKET_URL=http://localhost:5000
NEXT_PUBLIC_SOCKET_PATH=/socket.io

# Analytics / misc
NEXT_PUBLIC_ANALYTICS_ID=

# Notes:
# - Keep real secrets in .env.local (ignored by git).
# - For production, set env vars in your deployment platform (Vercel, Netlify, etc.).
